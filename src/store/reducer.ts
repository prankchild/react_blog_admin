// import global from './modules/global/reducer';

// const defaultState = {
//   ...global.state,
// };

// const reducer = (
//   state = defaultState,
//   action: { type: string; value: any }
// ) => {
//   let cloneState = JSON.parse(JSON.stringify(state));
//   switch (action.type) {
//     case 'add':
//       global.actions.add(cloneState, action);
//       break;
//   }
//   return cloneState;
// };

// export default reducer;
